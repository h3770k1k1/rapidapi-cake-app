import { createTheme } from "@mui/material";

export const theme = createTheme({
	palette: {
		primary: {
			light: "#dddddd",
			main: "#cccccc",
			dark: "#9a9a9a",
		},
		secondary: {
			main: "#ffc2cd",
		},
	},
});
